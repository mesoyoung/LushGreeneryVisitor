{
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "start": "^5.1.0"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
